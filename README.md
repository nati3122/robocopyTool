# robocopyTool

A simple Python script for performing file operations using Robocopy.

## Requirements

- Python 3.8+

## Setup

1. Install dependencies: `pip install -r requirements.txt`
2. Run the tool: `python main.py`

## Description

This repository contains a minimal Python application. The main entry point is `main.py`.

Feel free to expand the tool with additional functionality.
